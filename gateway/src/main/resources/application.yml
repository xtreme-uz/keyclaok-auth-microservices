
spring:
  application:
    name: gateway

  security:
    oauth2:
      resourceserver:
        opaquetoken:
          introspection-uri: http://localhost:8080/realms/master/protocol/openid-connect/token/introspect
          client-id: gateway-client
          client-secret: 73iMj0tfU4fY8jNQ9sWKCHXIe4pNgsKP

  cloud:
    gateway:
      globalcors:
        cors-configurations:
          '[/**]':
            allowedOrigins: "http://localhost:8181"
            allowedMethods:
              - GET
              - POST
              - PUT
              - PATCH
              - DELETE
            allowedHeaders: "*"

server:
  port: 8081

management:
  endpoints:
    web:
      exposure:
        include: "*"
